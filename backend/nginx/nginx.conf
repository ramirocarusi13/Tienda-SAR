worker_processes auto;

events {
    worker_connections 1024;
}

http {
    upstream microservicios {
        server api1:80;
        server api2:80;
        # server api3:80;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://microservicios;
        }
    }
}
